package ru.taskflow.assistant.api;

/**
 * Откуда пришло обращение внутри WEB-канала: диалог ассистента или кнопка
 * быстрого добавления. У Telegram такого различия нет — там всегда CHAT.
 * Влияет только на порядок альтернатив при двоякой трактовке (см. промпт),
 * не меняет схему инструментов и не запрещает никаких действий.
 */
public enum AssistantEntryPoint {
    CHAT, QUICK_ADD
}
